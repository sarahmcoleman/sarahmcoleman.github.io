\usepackage{enumitem}
\usepackage[top=0.45in, bottom=0.65in, left=0.55in, right=0.55in, footskip=0.3in]{geometry}
\usepackage{titlesec}

% Slightly heavier typeface than the default Latin Modern (full bold/italic family)
\usepackage{fontspec}
\setmainfont{Charter}

% Never break a word across lines with a hyphen
\hyphenpenalty=10000
\exhyphenpenalty=10000
\tolerance=2000
\emergencystretch=3em

\setlist[itemize]{noitemsep, topsep=0pt, parsep=0pt, partopsep=0pt}

% Tighten space around section headings
\titlespacing*{\section}{0pt}{7pt}{2pt}
\titlespacing*{\subsection}{0pt}{5pt}{1pt}

\setlength{\parskip}{0pt}
\setlength{\parindent}{0pt}

\linespread{0.97}
